# chain-of-thought
traceability matrix using ai audit trail , a middleware that sits between an LLM and end user. it records the prompt , retrives the data and the logic steps that generates a human readable audit showing exactly what piece led to the final decision
